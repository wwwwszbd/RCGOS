[package]
name =  "user_lib"
version = "0.1.0"
edition = "2024"

[dependencies]
buddy_system_allocator = "0.6"
riscv = { git = "https://github.com/rcore-os/riscv", features = ["inline-asm"] } 
bitflags = "1.2.1"

[profile.release]
debug = true

# [features]
# board_qemu = []
# board_k210 = []
